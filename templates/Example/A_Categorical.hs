-- | A (category) layer for the Template example: REUSE the library universes
--   (@MeasU@, @GeomU@). To use a different category, define it here instead.
module Examples.Template.A_Categorical
  ( module Lib.A_Categorical.CategoricalInterpretation,
  )
where

import Lib.A_Categorical.CategoricalInterpretation
