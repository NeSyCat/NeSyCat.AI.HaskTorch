-- | Category layer for the MNIST example: REUSE the library universes (@MeasU@,
--   @GeomU@). Swap here to use a different category.
module MnistAddition.A_Categorical
  ( module A_Categorical.CategoricalInterpretation,
  )
where

import A_Categorical.CategoricalInterpretation
