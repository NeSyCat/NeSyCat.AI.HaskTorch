-- | C (domain) interpretation for the Template example: assign the signature's
--   sorts/symbols to concrete objects/morphisms per universe (MeasU, GeomU). See
--   "Examples.Binary.C_Domain.Interpretation" for a worked example. Empty until filled.
module Examples.Template.C_Domain.Interpretation () where
